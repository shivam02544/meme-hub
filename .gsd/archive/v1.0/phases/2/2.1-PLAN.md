---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: Enhance Auth UI

## Objective
Apply the newly created skeuomorphic design tokens to the Authentication pages (`Login.jsx`, `Register.jsx`) and refine their existing OTP logic to ensure a seamless aesthetic integration.

## Context
- .gsd/SPEC.md
- frontend/src/pages/Login.jsx
- frontend/src/pages/Register.jsx
- frontend/src/index.css

## Tasks

<task type="auto">
  <name>Apply Skeuomorphic Styles to Login</name>
  <files>frontend/src/pages/Login.jsx</files>
  <action>
    - Wrap the login form in `.auth-wrapper` and `.auth-card`.
    - Apply `.form-group`, `.form-label`, `.form-input` to inputs and labels.
    - Transform the submit and OTP verify buttons into skeuomorphic `.btn.btn-primary`.
    - Handle loading states appropriately with disabled visuals.
  </action>
  <verify>grep -q "auth-card" frontend/src/pages/Login.jsx</verify>
  <done>Login page accurately reflects the skeuomorphic design system.</done>
</task>

<task type="auto">
  <name>Apply Skeuomorphic Styles to Register</name>
  <files>frontend/src/pages/Register.jsx</files>
  <action>
    - Ensure structure replicates Login with `.auth-wrapper` and `.auth-card`.
    - Update inputs to use `.form-input` and buttons to `.btn`.
    - Maintain existing OTP verification flow within the new UI layout.
  </action>
  <verify>grep -q "auth-card" frontend/src/pages/Register.jsx</verify>
  <done>Register page reflects the global skeuomorphic UI styles.</done>
</task>

## Success Criteria
- [ ] Both Login and Register screens utilize the `.auth-card` and `.btn` styled layouts.
- [ ] OTP forms fit naturally inside the skeuomorphic aesthetic without breaking flow.
