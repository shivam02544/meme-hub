---
phase: 6
plan: 2
wave: 2
---

# Plan 6.2: Public Profile Views

## Objective
Allow users to click on an author's name to view their public profile and all memes they have uploaded.

## Context
- .gsd/SPEC.md
- frontend/src/App.jsx
- frontend/src/pages/Dashboard.jsx
- routes.js

## Tasks

<task type="auto">
  <name>Implement User Memes API</name>
  <files>routes.js</files>
  <action>
    - Ensure `GET /api/memes` can filter by `userId` via query param.
    - Add `GET /api/users/:id/profile` to get public info (name, join date, meme count).
  </action>
  <verify>grep -q "userId" routes.js</verify>
  <done>Backend supports fetching memes for a specific user.</done>
</task>

<task type="auto">
  <name>Create Public Profile Page</name>
  <files>frontend/src/pages/UserProfile.jsx [NEW], frontend/src/App.jsx</files>
  <action>
    - Create `UserProfile.jsx` component to show a user's name and their meme grid.
    - Add route `/user/:id` in `App.jsx`.
    - Make author names on meme cards clickable links to their profile.
  </action>
  <verify>test -f frontend/src/pages/UserProfile.jsx</verify>
  <done>Users can browse memes uploaded by specific authors.</done>
</task>

## Success Criteria
- [ ] Clicking an author's name navigates to their collection.
- [ ] UserProfile page handles empty states and loads correct data.
