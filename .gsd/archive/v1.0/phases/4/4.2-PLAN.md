---
phase: 4
plan: 2
wave: 2
---

# Plan 4.2: Likes, Comments & Category Filter UI

## Objective
Add interactive like buttons, comment sections, and category filter dropdown to the Dashboard feed.

## Context
- .gsd/SPEC.md
- frontend/src/pages/Dashboard.jsx
- frontend/src/index.css

## Tasks

<task type="auto">
  <name>Add Like Button and Comment Section to Meme Cards</name>
  <files>frontend/src/pages/Dashboard.jsx</files>
  <action>
    - Add a heart/like button to each meme card that toggles on click (POST /api/memes/:id/like).
    - Show like count on each card.
    - Add a collapsible comments section under each meme card.
    - Include a small input form to post a new comment.
    - Fetch comments lazily on expand.
  </action>
  <verify>grep -q "like" frontend/src/pages/Dashboard.jsx</verify>
  <done>Each meme card has a working like toggle and comment section.</done>
</task>

<task type="auto">
  <name>Add Category Filter to Feed</name>
  <files>frontend/src/pages/Dashboard.jsx</files>
  <action>
    - Add a row of category filter buttons/badges above the meme grid.
    - Include an "All" option to show all memes.
    - Filter the displayed memes client-side based on selected category.
    - Style the active filter with a skeuomorphic inset shadow.
  </action>
  <verify>grep -q "filter" frontend/src/pages/Dashboard.jsx</verify>
  <done>Users can filter memes by category using skeuomorphic filter buttons.</done>
</task>

## Success Criteria
- [ ] Like button toggles and displays count on each meme.
- [ ] Comments can be added and viewed per meme.
- [ ] Category filter buttons filter the feed.
