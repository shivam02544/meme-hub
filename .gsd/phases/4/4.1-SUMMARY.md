# Plan 4.1 Summary
- Created `Likes` and `Comments` tables in Oracle DB with FK constraints and unique indexes.
- Implemented `POST /api/memes/:id/like` for atomic like/unlike toggling.
- Implemented `GET /api/memes/:id/comments` and `POST /api/memes/:id/comments` for flat discussions.
- Updated `GET /api/memes` with subqueries to return `likeCount` and `commentCount` for each meme.
