# Plan 6.1 Summary
- Updated `GET /api/memes` backend to support `q` (search), `sort` (trending/latest), and `categoryId`.
- Implemented server-side title/description searching with lowercase matching.
- Added a skeuomorphic Search Bar at the top of the feed in `Dashboard.jsx`.
- Added a "Latest / Trending" sort toggle with inset styling for the active state.
- Integrated debounced search (400ms) to ensure smooth performance without overloading the database.
- Switched from client-side filtering to full server-side filtering for better scalability.
