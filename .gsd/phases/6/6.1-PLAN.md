---
phase: 6
plan: 1
wave: 1
---

# Plan 6.1: Search & Trending Sort (Backend & Frontend)

## Objective
Implement serverside search by title and sorting by like count to allow users to discover popular content.

## Context
- .gsd/SPEC.md
- routes.js
- frontend/src/pages/Dashboard.jsx

## Tasks

<task type="auto">
  <name>Implement Search & Sort in GET /memes</name>
  <files>routes.js</files>
  <action>
    - Add query parameters `q` (search title) and `sort` (trending vs latest) to `GET /api/memes`.
    - Update SQL query to use `WHERE title LIKE :q` if provided.
    - Update `ORDER BY` clause based on `sort` param (either `like_count DESC` or `created_at DESC`).
  </action>
  <verify>grep -q "q" routes.js && grep -q "sort" routes.js</verify>
  <done>Search and Trending sort are functional on the backend.</done>
</task>

<task type="auto">
  <name>Add Search Bar and Sort Toggle to Dashboard</name>
  <files>frontend/src/pages/Dashboard.jsx</files>
  <action>
    - Add a skeuomorphic search input at the top of the feed.
    - Add a "Latest" vs "Trending" toggle button.
    - Debounce search input to avoid excessive API calls.
  </action>
  <verify>grep -q "search" frontend/src/pages/Dashboard.jsx</verify>
  <done>Users can search memes and sort by popularity in the Dashboard UI.</done>
</task>

## Success Criteria
- [ ] Users can find memes by typing in the search bar.
- [ ] Users can toggle between "Latest" and "Trending" views.
