---
phase: 3
plan: 1
wave: 1
---

# Plan 3.1: Enhanced Category UI

## Objective
Refactor the category selection bar to use a premium glassmorphism design with clear active states and integrated counts.

## Context
- .gsd/SPEC.md
- d:\DBT-Activity\frontend\src\pages\Dashboard.jsx
- d:\DBT-Activity\frontend\src\index.css

## Tasks

<task type="auto">
  <name>Implement Category Bar Styles</name>
  <files>frontend/src/index.css</files>
  <action>
    - Define `.category-bar` (horizontal scroll container).
    - Define `.category-chip` (individual filter pill).
    - Add active state styles using `--glass-glow` and `--primary-color`.
  </action>
  <verify>Check index.css for new classes.</verify>
  <done>Styles exist and use design tokens.</done>
</task>

<task type="auto">
  <name>Refactor Dashboard Category Section</name>
  <files>frontend/src/pages/Dashboard.jsx</files>
  <action>
    - Replace the current categories list with the new `.category-bar` and `.category-chip` structure.
    - Integrate `stats` to show meme counts next to each category name.
    - Ensure "All" persists as a distinct filter.
  </action>
  <verify>Visually check categories bar in browser.</verify>
  <done>Categories bar is responsive and shows counts.</done>
</task>

## Success Criteria
- [ ] Categories are horizontally scrollable on small screens.
- [ ] Active category has a distinct glow and primary background.
- [ ] Meme counts are visible for each category.
